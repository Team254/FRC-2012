import socket
import time
UDP_IP="127.0.0.1"
UDP_PORT=41234

 
sock = socket.socket( socket.AF_INET, # Internet
                       socket.SOCK_DGRAM ) # UDP
for i in range(0,1):
    msg='{"S":%d, "V":40}' % 50
    sock.sendto( msg, (UDP_IP, UDP_PORT) )
    time.sleep(.25)
